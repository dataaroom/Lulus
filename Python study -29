# python3
# 题目：一个5位数，判断它是不是回文数。即12321是回文数，个位与万位相同，十位与千位相同。


def reverse(num):
    rev_num = ''
    for l in range(len(num)):
        rev_num.append(num[len(num)-i]) 
    return rev_num
     

n = input('Pick a number.(10000 ~ 90000)')      # 没有对输入进行检查。 任何字符串都可以进行比较。

if n == reverse(n):
    print (n, ' is huiwenshu.')
else:
    print (n, 'is not huiwenshu.')
